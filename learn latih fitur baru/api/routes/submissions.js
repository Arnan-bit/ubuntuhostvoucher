// api/routes/submissions.js
import express from "express";

const router = express.Router();

// Route-route untuk submission (voucher, deal request, dll) akan ditambahkan di sini

export default router;

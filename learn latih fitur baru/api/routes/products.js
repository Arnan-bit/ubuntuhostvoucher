// api/routes/products.js
import express from "express";

const router = express.Router();

// Route-route untuk produk akan ditambahkan di sini
// contoh: router.get("/", getAllProducts);

export default router;
